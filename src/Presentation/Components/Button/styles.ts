import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    button: {
        alignItems: 'center',
        justifyContent: 'center',
        borderRadius: 5,
        height: 50,
    },
    loadingText: {
        color: '#fff',
    },
    buttonContent: {
        flexDirection: 'row',
        alignItems: 'center'
    },
    text: {
        color: '#fff',
    },
});
